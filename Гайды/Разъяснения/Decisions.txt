# Значок "#" это заметка (коментарий если по правильному) для разроботичков, котрые не считаваются програмой, в нашем случае хойке. Можно удалить)

# Category

# Базовая структура
Name_category = {   # по сути тоже id что и у фокусов

    icon = generic_construction     # иконка, брать из gfx\interface\decisions
    priority = 90   # приоритет в меню решений (как выскоко будет эта категория, просто повышайте занчение до желаемого результата)
    visible_when_empty = yes    # показывать когда пустой

    allowed = {     # блок "разрешено" проверяется при заходе в игру или созранение, в основном прописываются страны у которых будет эта категория
        original_tag = AUS # разрешено если Австрия
    }
}

# Для подсветки регионов на карте используется:

on_map_area = { 
    state = 123 # стейты для подсветки (может быть несколко)
	state = 321 # вот так добавлять несколько
    Zoom = 850  # на сколько будет приближение к первому стэйту (в нашем случае 123)
}

# Decisions

Name_category = {   # обязательно все решения пишутся в блоке с названием нужной категории

    Name_decisions = {      # id решения
        visible = {has_government = communism}      # будет виден если...

        priority = 10   # приоритет в категории (как высоко будет эта категория, просто повышайте занчение до желаемого результата)

        icon = generic_intelligence_operation   # иконка, брать из gfx\interface\decisions

        available =  {  # станет доступным к прожатию (зелёным) если...
            communism > 0.3
        }

		
		
        complete_effect = {add_stability = 0.1} # эффекты после прожатия (по сути как completion_reward у фокусов)

        days_mission_timeout = 30   # время действия миссии
		
		modifier = { # модификаторы как у нац. духов 
            fascism_drift = -0.01 # ежеднвное изменение популярности идеологии фашизм на -0.01 в день
        }

		timeout_effect = {  # эффект после истечения времени
			add_popularity = {
				ideology = fascism
				popularity = -0.1
			}
		}
		
        cost = 150  # стоимость в политической власти
    }
}